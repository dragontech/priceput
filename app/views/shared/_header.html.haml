#authentication
  - if user_signed_in?
    = link_to(current_user.name, profile_users_path) 
    = link_to('logout', destroy_user_session_path, 'data-method' => :delete)
  - else
    = link_to('login', new_user_session_path)
